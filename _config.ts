import type { HonoBlogOptions } from "./src/configuration.ts";

const config: HonoBlogOptions = {
  siteName: "Pho Thin",
  baseDir: "app",
  meta: {
    favicon: "favicon.ico",
  },
};

export default config;
